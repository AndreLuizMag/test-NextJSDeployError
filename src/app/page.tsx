import styles from "./page.module.css";

const Home = () => {
  return (
    <main>
      <section>
        <div className="container-sm">
          <h1 className={styles.title}>Test link</h1>
        </div>
      </section>
    </main>
  );
};

export default Home;
